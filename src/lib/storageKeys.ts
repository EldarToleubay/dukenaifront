export const initialStockSkippedKey = (storeId: string) =>
  `rms_onboarding_initial_stock_skipped_${storeId}`;

export const initialStockDoneKey = (storeId: string) =>
  `rms_onboarding_initial_stock_done_${storeId}`;
